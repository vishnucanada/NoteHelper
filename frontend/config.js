// Frontend runtime config.
// After deploying the backend to Fly.io, set this to your Fly app URL, e.g.
//   window.API_BASE = 'https://notehelper-api.fly.dev';
// Leave it null to use the local Flask server (http://127.0.0.1:5000) during development.
window.API_BASE = null;
